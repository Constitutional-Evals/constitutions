# Changelog

Repo-level changes. Per-constitution history is in each file's `changelog`; basis history is
tracked via `basis_version` in `anchors/anchor-set.json`.

## [Unreleased]
- Schema 1.1: added `overview`, four-voice `voices` (1st/2nd/3rd person), `guidelines`,
  `valence` (aligned/misaligned/neutral), and `provenance.source_bundle`.
- Added `sources/` (foundational documents, with a redistributable/license guard) and
  `generation/` (the single shared meta-prompt + methodology).
- Confirmed Ahimsa and Scientific Rationalism as `supplementary`; added misaligned anchors.
- Initial repository scaffolding: structure, schema, versioning contract, CI.
